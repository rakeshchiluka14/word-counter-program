# word-counter program instructions


* `mvn compile`: Compile module.
* `mvn test`:  run tests cases. 
* `mvn install`: installs application.

word-counter program to count the number of words in a given sentence.
